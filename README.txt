Project: PETERNAKAN HEWAN
Mockup Package: v0.1 (UI-only)

Isi:
- index.html (shell home untuk cek ukuran/rapih)
- login.html (mockup UI login)
- register.html (mockup UI daftar)
- css/ui.css (layout, ukuran terkunci, responsive)
- js/ui.js (notif/lang popup, theme toggle, spinner demo)
- img/* (ikon)

Catatan:
- Ini mockup visual — belum ada Firebase/Firestore.
- Setelah kamu OK, langkah berikut: v0.2 (Auth terhubung Firebase).
- Versi akan selalu lengkap (bukan placeholder). Tiap kiriman mencakup file baru + file yang berubah dari versi sebelumnya.